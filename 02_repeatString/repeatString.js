const repeatString = function(stringE, num) {
	let newString = "";

	for (let i = 0; i < num; i++)
		newString += stringE;

	return newString;
};

// Do not edit below this line
module.exports = repeatString;
